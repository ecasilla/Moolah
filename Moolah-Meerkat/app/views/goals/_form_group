# user.connections.each do |connection|
# a list of all connections by name. 
# a button for each connection to be added to the group. 
<%= link_to "Add to goal", user_connections_path(:friend_id => user), :method => :post %>